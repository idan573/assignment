import styled from 'styled-components';

export const StyledUploadImageStep = styled.div`
  height: var(--activeScreenHeight);

  img {
    max-height: 400px;
    max-width: 400px;
    justify-self: center;
  }
`;
