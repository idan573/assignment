import styled from 'styled-components';

export const StyledChapterTimelinePage = styled.section`
  padding: 24px 16px var(--headerHeight);

  display: grid;
  grid-gap: 16px;

  & > h2 {
    text-align: center;
  }
`;
